import {prisma} from "../lib/prisma";
export default async function Home(){
 const tournaments=await prisma.tournament.findMany({where:{status:"OPEN"},orderBy:{eventDate:"asc"},take:6});
 return <><nav className="nav"><div className="brand"><span>e</span>Sports Wala</div><div className="navlinks"><a href="/">Home</a><a href="/admin">Admin</a></div><a className="btn" href="/register">Register Team</a></nav>
 <section className="hero"><div><div className="eyebrow">FREE FIRE • ESPORTS • INDIA</div><h1>Play. Compete.<strong>Become Champion.</strong></h1><p className="muted">Tournament registration, team management and rankings for an independent esports community.</p><a className="btn" href="#tournaments">Explore Tournaments</a></div><div className="panel"><div className="eyebrow">LIVE PLATFORM</div><h2>eSports Wala</h2><p className="muted">Run competitive events with a proper backend and database.</p><a className="btn" href="/register">Join Tournament</a></div></section>
 <section id="tournaments" className="section"><div className="eyebrow">COMPETE</div><h2>Upcoming Tournaments</h2><div className="grid">{tournaments.length?tournaments.map(t=><div className="card" key={t.id}><div className="eyebrow">{t.status}</div><h3>{t.name}</h3><p className="muted">Squad • {t.slots} Slots</p><h3>{t.prizePool}</h3><p className="muted">{new Date(t.eventDate).toLocaleString("en-IN")}</p><a className="btn" href="/register">Register</a></div>):<div className="card"><p className="muted">No tournaments yet. Add one from your database/admin workflow.</p></div>}</div></section>
 <footer><div className="brand"><span>e</span>Sports Wala</div><p>Independent esports tournament platform. Not affiliated with Garena.</p></footer></>}
